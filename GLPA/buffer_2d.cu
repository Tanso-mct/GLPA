#include "buffer_2d.cuh"

// void gpuInputPixelRgba(LPDWORD *calcData, LPDWORD *targetBuffer, int caldDataPixels){
//     int i = blockIdx.y * blockDim.y + threadIdx.y;
//     int j = blockIdx.x * blockDim.x + threadIdx.x;

//     // calcData[pixel_color], []

//     // if (i < calcData[])
// }
